<div class="alertas">
    <p class="alerta warning">Por seguridad, la fecha, hora y cupo del evento deben volver a indicarse. </p>
</div> 
<main class="contenedor contenedor-formularios">
    <h1><%- pageName %></h1>
    <form class="default-form" method="POST">
        <legend>Sobre el Meeti</legend>
        <div class="campo">
            <label>Grupo</label>
            <select name="groupId">
                    <option value="" disabled selected>-- Selecciona Grupo --</option>
                    <% groups.forEach( group => {  %>
                        <option <%= (meet.groupId === group.id) ? 'selected' : '' %>  value="<%= group.id %>"><%= group.name %> </option>
                    <% }) %>  
            </select>
        </div>
        <div class="campo">
            <label>Titulo</label>
            <input type="text" name="title" value="<%= meet.title %> ">
        </div>
        <div class="campo">
            <label>Invitado</label>
            <input type="text" name="guest" value="<%= meet.guest %> ">
        </div>
        <div class="columnas-2 grid">
            <div class="campo">
                <label>Fecha</label>
                <input type="date" name="date" value="<%= meet.date %> ">
            </div>
            <div class="campo">
                <label>Hora</label>
                <input type="time" name="time" value="<%= meet.time %> ">
            </div>
        </div>
        <div class="campo">
            <label>Cupo</label>
            <input type="number" min="1" name="quota" value="<%= meet.quota %> ">
        </div>
        <div class="campo descripcion">
            <label>Descripción</label>
            <div class="contenedor-editor">
                <input type="hidden" id="x" name="description" value="<%= meet.description %> ">
                <trix-editor input="x"></trix-editor>
            </div>
        </div>
        <legend>Ubicación Meeti</legend>
        <div class="campo buscador">
            <label>Coloca la Dirección del Meeti</label>
            <input type="text" id="formbuscador" placeholder="Ubicación">
        </div>
        <div class="contenedor-input">
            <small>El asistente colocará una dirección estimada</small>
        </div>
        <div class="campo contenedor-mapa">
            <div id="mapa"></div>
        </div>
        <p class="informacion">Mové el pin hacia el punto exacto en caso de ser necesario</p>
        <p class="informacion">Confirma que los siguientes campos sean correctos:</p>

        <div class="campo">
            <label>Dirección</label>
            <input type="text" id="direccion" name="address" value="<%= meet.address %> ">
        </div>
        <div class="campo">
            <label>Ciudad</label>
            <input type="text" id="ciudad" name="city" value="<%= meet.city %> ">
        </div>
        <div class="campo">
            <label>Estado</label>
            <input type="text" id="estado" name="state" value="<%= meet.state %> ">
        </div>
        <div class="campo">
            <label>País</label>
            <input type="text" id="pais" name="country" value="<%= meet.country %> ">
        </div>
        <div class="campo">
            <label>Latitud</label>
            <input type="text" id="lat" name="lat" value="<%= meet.location[0] %> ">
        </div>
        <div class="campo">
            <label>Longtitud</label>
            <input type="text" id="lng" name="lng" readonly="readonly" value="<%= meet.location[1] %> ">
        </div>
        <div class="campo enviar-cancelar">
            <a href="/admin" class="btn btn-rosa">Cancelar</a>
            <input type="submit" class="btn btn-rosa" value="Guardar Cambios">
        </div>     
        <div class="campo enviar">
        </div>        
    </form>
</main>