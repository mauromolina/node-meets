<div class="hero">
    <h1>Encontrá o creá un grupo para compartir lo que más te gusta</h1>
    <a href="/signup" class="btn btn-amarillo">Obtener Cuenta</a>
</div>

<div class="buscador-inicio">
    <div class="contenedor">
        <h2>Busca un grupo en tu ciudad</h2>
        <form action="/search" method="GET">
            <select name="category">
                    <option value="" selected>Cualquiera</option>
                    <% categories.forEach( category => {  %>
                        <option value="<%= category.id %>"><%= category.name %> </option>
                    <% }) %>  
            </select>
            <input type="text" name="title" placeholder="Nombre Evento">
            <input type="text" name="city" placeholder="Ciudad">
            <select name="pais">
                <option value="" selected>-- Todos --</option>
                <option value="MEX">México</option>
                <option value="COL">Colombia</option>
                <option value="ARG">Argentina</option>
                <option value="ESP">España</option>
                <option value="USA">Estados Unidos</option>
                <option value="PER">Perú</option>
            </select>
            <input type="submit" value="Buscar" class="btn btn-rosa">
        </form>
    </div>
</div>

<div class="contenedor">
    <h2>Próximos Eventos</h2>
    <div class="grid columnas-md-3 ">
        <% if (meets.length > 0) { %>
         <% meets.forEach(meet => { %>
            <div class="card">
                <a href="/meet/<%= meet.slug %> ">
                    <img src="/uploads/groups/<%= meet.group.image %> ">
                </a>
                <div class="card-texto">
                    <% moment.locale('es') %>
                    <% const date = meet.date + ' ' + meet.time %>  
                    <p class="fecha"><%= moment(date).format('LLLL') %>hs </p>
                    <a href="/meet/<%= meet.slug %> ">
                        <h3><%= meet.title %> </h3>
                    </a>
                    <div class="info-autor">
                        <% if (meet.user.image) { %>
                            <div class="imagen">
                                <img class="profile-info" src="/uploads/profiles/<%= meet.user.image %> " alt="">
                            </div>
                        <% } else { %>
                            <div class="imagen"></div>
                        <% } %> 
                        <div class="informacion">
                            <p>Organizado por:</p>
                            <p class="autor"><%= meet.user.name %> </p>
                        </div>
                    </div>
    
                </div>
            </div>
         <% }) %>
        <% } %>
    </div>
</div>

<main class="bg-gris categorias">
    <h2>Encontrá Grupos por Categoría</h2>

    <div class="contenedor">
        <ul class="listado-categorias  grid columnas-2 columnas-md-3">
            <% categories.forEach( category => {  %>
                <li>
                    <a href="/category/<%= category.slug %> ">
                        <img src="/img/categoria_<%= category.id %>.jpg">
                        <p><%= category.name %> </p>
                    </a>
                </li>
            <% }) %> 
        </ul>
    </div>
</main>

<div class="meeti-bg">
    <div class="contenedor meeti grid columnas-md-2">
        <section>
            <h3>Crea un grupo</h3>
            <p>Compartí lo que te gusta o tu pasión con los demás, creá tu propio grupo hoy mismo sin costos</p>
        </section>
        <section>
            <h3>Unite a un grupo</h3>
            <p>Conocé personas con tus mismos gustos y pasiones, unite a un grupo ya existente y recibí notificaciones sobre próximos eventos</p>
        </section>
    </div>
</div>
