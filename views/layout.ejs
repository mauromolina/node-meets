<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://necolas.github.io/normalize.css/8.0.1/normalize.css">
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@400;700;900&family=Staatliches&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/trix.css">
    <link rel="stylesheet" href="/css/app.css">
    <title>Meets - <%- pageName %></title>
</head>
<body>
    
    <header class="site-header contenedor">
        <a href="/">
            <img src="/img/logo.png" alt="Logo Meeti">
        </a>
        <nav class="nav">
            <a href="/newGroup">Crear grupo</a>
            <a href="/login">Iniciar sesión</a>
            <a href="/signup">Registrarse</a>
        </nav>
    </header>

    <% if(locals.msg) { %>
        <% const category = Object.keys(locals.msg) %> 
        <% if (category.length) { %>
         <% locals.msg[category].forEach( alert => { %> 
            <div class="alertas">
                <p class="alerta <%= category %> "><%= alert %> </p>
            </div> 
        <% }) %> 
        <% } %>
    <% } %>

    <%- body %>
    <footer class="site-footer contenedor">
        <nav class="nav">
            <a href="/newGroup">Crear grupo</a>
            <a href="/login">Iniciar sesión</a>
            <a href="/signup">Registrarse</a>
        </nav>
        <p class="copyright">
            Todos los derechos reservados Meeti <%- year %> &copy;
        </p>
    </footer>
    <script src="/js/trix.js"></script>
</body>
</html>